# Deployment

> Status: stub. To be completed alongside `deploy.sh` in v0.6.0 (Operations phase).

This document will describe the deployment flow (Git pull → Docker pull → Compose up → health check → rollback) and how to trigger deployments per environment.

See [project-specification.md](project-specification.md) for the current high-level roadmap.
