services:
  nginx:
    restart: always
  app:
    restart: always
  web:
    restart: always
  # Same bounded-retry reasoning as staging (see docker-compose.staging.yml) -
  # postgres never gets an unbounded restart loop.
  postgres:
    restart: on-failure:5
